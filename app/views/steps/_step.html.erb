<h4><%=step.title%></h4>
<%= link_to('Edit', edit_project_section_task_step_path(@project, @section, @task, step)) %>
<%= link_to('Delete', project_section_task_step_path(@project, @section, @task, step), method: :delete, data: {confirm: 'Are you sure?'}) %>
<h5><%=step.description%></h5>