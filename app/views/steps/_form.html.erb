<%= form_for([task, step]) do |form| %>
  <div>
    <% if step.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@step.errors.count, "error") %> prohibited this post from being saved:</h2>

        <ul>
        <% @step.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :title,'Enter your step title'%>
      <%= form.text_field :title, placeholder: "Step title..."%>
    </div>

    <div>
      <%= form.label :description, 'Enter a description of the step'%>
      <%= form.text_area :description %>
    </div>

     

        <div>
          <%= form.label :file, 'attach any resources associated to this step'%>
          <%= form.file_field :file %>
        </div>

      
  



    <div>
      <%= form.submit 'Add!', class: 'actions'%>
    </div>
  </div>
<% end %>