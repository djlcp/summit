<div class="container bg-white border rounded py-4 px-4">
	<h2>Create a New Project</h2>

	<%= form_with(model: @project, local: true) do |form| %>

		<%= render 'form', project: @project %>

	<% end %>
</div>